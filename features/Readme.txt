Directory Structure
  features: Holds all features files (.feature) which act as instructions/documentation for the tests

  step_definitions: Holds all step definitions which are act as code behind the feature files

  library: Holds all support classes which contain code common to multiple step definitions

  support: Holds environment/project configurations

  test_data: Holds any data used it executing tests

  test_results: Holds any logged results from tests